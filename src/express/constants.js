'use strict';

const DEFAULT_PORT = 8080;
const Dir = {
  PUBLIC: `public`,
  UPLOAD: `upload`
};

const PUBLICATIONS_PER_PAGE = 8;

module.exports = {
  DEFAULT_PORT,
  Dir,
  PUBLICATIONS_PER_PAGE
};

